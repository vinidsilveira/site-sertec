{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"c:/ws/ws-visualstudio/projetos/site-sertec\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: sync changes\" && git push origin main)",
            "statusMessage": "Sincronizando com GitHub...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
